<table class="list w3-table-all">
    <thead>
        <tr>
            <th>Order Number</th>
            <th>User</th>
            <th>Date</th>
        </tr>
    </thead>
    <tbody>
        {% for scan in scans %}
            <tr>
                <td>{{ scan.orderNumber }}</td>
                <td>{{ scan.user }}</td>
                <td>{{ scan.timestamp|date('D, d M Y h:i:s a') }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>
